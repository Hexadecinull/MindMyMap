{
  "name": "mindmymap-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "MindMyMap monorepo \u2014 web app, desktop app, and browser extension",
  "license": "AGPL-3.0",
  "scripts": {
    "dev": "npm run dev       --prefix web",
    "build": "npm run build     --prefix web",
    "build:dist": "npm run build:dist --prefix web",
    "lint": "npm run lint      --prefix web",
    "desktop": "npm start         --prefix desktop",
    "desktop:build": "npm run build:all --prefix desktop"
  },
  "engines": {
    "node": ">=18"
  }
}
